import React from "react";
import styled from "styled-components";

// 새로운 일정을 생성하는 페이지
const NewSchedule = () => {
  return <StyledWrapper>NewSchedule</StyledWrapper>;
};

export default NewSchedule;

const StyledWrapper = styled.div`
  position: absolute;
`;
